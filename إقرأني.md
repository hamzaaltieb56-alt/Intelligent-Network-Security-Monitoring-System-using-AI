# نظام مراقبة أمن الشبكات الذكي باستخدام الذكاء الاصطناعي 🛡️🤖

هذا المشروع هو نظام متكامل لمراقبة أمن الشبكات يهدف إلى اكتشاف التهديدات السيبرانية (مثل هجمات DDoS، Port Scanning، و Brute Force) في الوقت الفعلي باستخدام تقنيات تعلم الآلة (Machine Learning).

---

## 📁 هيكلية المشروع (Project Structure)

1. **`data/`**: المجلد المخصص لتخزين البيانات (سواء ملفات PCAP أو ملفات CSV المولدة للتدريب).
2. **`models/`**: يحتوي على نماذج الذكاء الاصطناعي المدربة (joblib files).
3. **`logs/`**: يحتوي على سجل التنبيهات (`alerts.log`) الذي يسجل الهجمات المكتشفة.
4. **`reports/`**: يحتوي على تقارير أمنية دورية بصيغة Markdown.
5. **`templates/`**: يحتوي على واجهة المستخدم (Dashboard) المصممة بلغة HTML.
6. **`src/`**: المجلد الرئيسي الذي يحتوي على الكود البرمجي (Python).

---

## 💻 شرح المكونات البرمجية (`src/`)

### 1. معالجة البيانات واستخراج الميزات
- **`data_collector.py`**: مسؤول عن قراءة حركة الشبكة (Traffic) وتحويلها إلى تدفقات بيانات (Flows).
- **`feature_extractor.py`**: يستخرج الخصائص الرياضية من الحزم (مثل حجم الحزمة، بروتوكول الاتصال، والوقت بين الحزم).
- **`preprocessing.py`**: ينظف البيانات ويجهزها لتكون مناسبة لنماذج الذكاء الاصطناعي (Scaling & Encoding).

### 2. نماذج الذكاء الاصطناعي (AI Models)
- **`anomaly_model.py`**: يستخدم خوارزمية **Isolation Forest** لاكتشاف السلوكيات الغريبة (Anomalies) التي لم يسبق للنظام رؤيتها.
- **`classifier_model.py`**: يستخدم خوارزمية **Random Forest** لتصنيف نوع الهجوم بدقة (هل هو DDoS أم Port Scan؟).
- **`train_model.py`**: السكربت المسؤول عن تدريب النظام على البيانات وحفظ النماذج في مجلد `models/`.

### 3. المراقبة والتنبيه
- **`realtime_monitor.py`**: المحرك الرئيسي الذي يعمل في الخلفية لمراقبة البيانات في الوقت الفعلي واكتشاف الهجمات فور وقوعها.
- **`alerts.py`**: نظام التنبيهات الذي يحدد خطورة الهجوم (Critical, High, Medium) ويحسب "درجة التهديد" (Threat Score).

### 4. الواجهة والتقارير (Interface & Reporting)
- **`dashboard.py`**: تطبيق Flask يشغل لوحة تحكم تفاعلية لعرض الهجمات المكتشفة مباشرة.
- **`reports_generator.py`**: يقوم بتحليل سجلات التنبيهات وإنشاء تقرير إحصائي مفصل للمدير الأمني.

---

## 🚀 كيف تشغل المشروع؟

اتبع هذه الخطوات بالترتيب:

1. **تثبيت المكتبات المطلوبة**:
   ```bash
   pip install -r requirements.txt
   ```

2. **تدريب النظام (مرة واحدة في البداية)**:
   ```bash
   python src/train_model.py
   ```

3. **تشغيل المراقبة (Simulated Monitoring)**:
   ```bash
   python src/realtime_monitor.py
   ```

4. **تشغيل لوحة التحكم (Dashboard)**:
   ```bash
   python src/dashboard.py
   ```
   *بعدها افتح المتصفح على: `http://localhost:5000`*

5. **توليد تقرير أمني**:
   ```bash
   python src/reports_generator.py
   ```

---

## 🛠️ التقنيات المستخدمة
- **Python**: اللغة الأساسية.
- **Scikit-Learn**: لبناء نماذج الذكاء الاصطناعي.
- **Pandas & Numpy**: لمعالجة البيانات الضخمة.
- **Flask**: لبناء لوحة التحكم.
- **Scapy**: للتعامل مع حزم الشبكة (Packets).

تم تصميم النظام ليكون مرناً وقابلاً للتطوير ليناسب مختلف أحجام الشبكات.
