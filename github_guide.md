# 🚀 دليل رفع المشروع على GitHub

لقد قمت بتجهيز كافة الإعدادات اللازمة (Git, .gitignore, LICENSE, README). لرفع المشروع على حسابك في GitHub، اتبع الخطوات التالية:

### 1. إنشاء مستودع جديد (New Repository)
- اذهب إلى [github.com/new](https://github.com/new).
- أعطِ المستودع اسماً مثل `intelligent-network-security-ai`.
- اتركه **Public** لا تضف (README أو .gitignore أو License) لأنني قمت بإنشائها لك بالفعل.
- اضغط على **Create Repository**.

### 2. ربط المشروع المحلي بالمستودع البعيد
افتح التيرمينال داخل مجلد المشروع وقم بتشغيل الأوامر التالية (استبدل `your-username` باسم حسابك):

```bash
# إضافة كافة الملفات للجاهزية
git add .

# تسجيل التغييرات (Commit)
git commit -m "إطلاق أول نسخة من نظام أمن الشبكات الذكي"

# تحديد الفرع الرئيسي
git branch -m main

# ربط المستودع (استبدل الرابط برابط المستودع الخاص بك)
git remote add origin https://github.com/your-username/intelligent-network-security-ai.git

# رفع الملفات
git push -u origin main
```

### 3. ماذا تم استبعاده؟ (.gitignore)
لقد قمت ببرمجة ملف `.gitignore` ليستبعد الملفات التالية تلقائياً عند الرفع:
- **`models/`**: لأن أحجام نماذج الذكاء الاصطناعي قد تكون كبيرة جداً على GitHub (يفضل تدريبها عند كل تنزيل للمشروع).
- **`logs/` & `reports/`**: لعدم رفع بيانات المراقبة الخاصة بجهازك.
- **`data/`**: لاستبعاد ملفات البيانات الضخمة.
- **`__pycache__/`**: ملفات بايثون المؤقتة.

---
مبروك! مشروعك الآن جاهز ليكون واجهة احترافية في معرض أعمالك.
